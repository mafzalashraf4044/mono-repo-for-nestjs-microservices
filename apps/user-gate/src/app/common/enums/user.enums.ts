export enum UserStatus {
  NotVerified = 'NotVerified',
  Active = 'Active',
  InActive = 'InActive',
  Deleted = 'Deleted',
}

export enum UserRole {
  Member = 'Member',
  Admin = 'Admin',
}

export enum UserGender {
  Male = 'Male',
  Female = 'Female',
  NonBinary = 'NonBinary',
}
